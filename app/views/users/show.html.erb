<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
  
</p>
<p>
  <strong>Posts:</strong>
   <% @microposts.each_with_index do |post, i| %>
        <p><%= "Number #{i + 1}: #{post.content}" %></p>
        <p><%= "Created: #{post.created_at}" %></p>
        <p><%= "Updated: #{post.updated_at}" %></p>
        <br>
    <% end %>
  
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
